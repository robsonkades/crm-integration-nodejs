import { SQSHandler } from 'aws-lambda';

export const initialCharge: SQSHandler = async (event, context) => {
  if (event.Records) {
  }
};

export const blingIntegration: SQSHandler = async (event, context) => {
  if (event.Records) {
  }
};
